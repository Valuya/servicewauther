{
  "name": "@servicewauther/servicewauther",
  "version": "0.0.0",
  "description": "Servicewauther",
  "scripts": {
    "build": "npm run build:prod",
    "build-and-publish": "npm run build:dev && npm publish",
    "build:dev": "webpack --mode=development",
    "build:prod": "webpack --mode=production --node-env=production"
  },
  "dependencies": {
    "@panva/oauth4webapi": "^1.1.0",
    "tslib": "2.4.0"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "ts-loader": "^9.3.1",
    "typescript": "^4.6.4",
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0"
  }
}
